spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jpa_course
    username: postgres
    password: ecuacion
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true
